# Copy to .env and set your secrets
# Server-side token (checked in WebSocket server)
WS_AUTH_TOKEN=change-me

# Client-side token (exposed to browser; must match server token)
PUBLIC_WS_TOKEN=change-me
